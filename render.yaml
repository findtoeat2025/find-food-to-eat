services:
  - type: web
    name: find-food-to-eat
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
    healthCheckPath: /
    autoDeploy: true 